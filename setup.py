from setuptools import setup, find_packages

setup(name='haplm', 
      version='1.0', 
      author='Yong See Foo',
      dsecription='Bayesian inference for pooled haplotype data using latent multinomials',
      packages=['haplm'])
